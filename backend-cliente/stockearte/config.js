// Configuración del servidor gRPC
const config = {
    grpcServer: {
        // Dirección y puerto del host donde se ejecuta el servidor gRPC
        host: 'localhost',
        port: 9090
    }
};

module.exports = config;
